{
  "name": "@road/simulator",
  "version": "0.1.0",
  "private": true,
  "description": "Simulatore di flotta (MILESTONES.md §M7): avanza i veicoli lungo la rotta comandata su tick() espliciti. È l'adapter della flotta, raggiunto solo attraverso ExternalServicesPort.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "clean": "tsc -b tsconfig.build.json --clean"
  },
  "dependencies": {
    "@road/shared": "workspace:*"
  }
}
